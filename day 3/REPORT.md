# День 3. Testing — отчёт

## Ход дня

1. **Уровень 1** — один промпт агенту: «найди непокрытые модули, напиши тесты,
   добейся зелёного». Результат с первого прогона: 3 тестовых файла, 25 тестов,
   25/25. Для тестируемости агент сделал минимальный seam: `fun interface
   ChatClient`, который реализует `DeepSeekClient`, — интеграционный тест
   поднимает настоящий `HttpServer` на порту 0 с фейковым LLM и бьёт по ветвям
   обороны реальными HTTP-запросами.
2. **Деплой фичи №1** (day-builder): веб-UI мотиватора (`WebUi.kt`, маршрут
   `GET /`) со стабильными id для автотестов. +3 теста (28).
3. **Уровень 2** — сценарии `smoke-scenarios.md` (S1–S4) и раннер
   `smoke/run_smoke.py`: реальный Chrome, скриншот каждого шага, вердикты.
   Прогон v1: **3/4 — S4 упал и нашёл настоящий баг**: пустая задача молча
   глоталась (`if (!task) return;` в JS + нативный `required` перехватывал
   submit) — ни ошибки, ни фразы. Раннер сам указал сломавшийся шаг и приложил
   скриншот падения.
4. **Фикс через профиль bug-fix** (конвейер дня 2 в действии): `novalidate` на
   форме + человекочитаемая ошибка в `#errorBox`; юнит-тесты не тронуты.
   Перепрогон: 4/4.
5. **Усиление, сценарий «задеплоил новую фичу»**: day-builder добавил очистку
   истории (`clear()` + `DELETE /v1/history` + кнопка `#clearHistoryBtn`,
   +5 тестов → 33). Сценарии обновлены до v2 (+S5), раннер расширен, полный
   перепрогон: **5/5**.
6. **Усиление, flow**: скилл `post-pr-check` — единый прогон уровня 1 + уровня 2
   с единым отчётом. Прогнан целиком: 33/33 + 5/5 → `pr-check-report.md`,
   вердикт MERGE OK.

## Что дал каждый уровень

- Код-тесты поймали бы регрессии логики (кольцо истории, ветви обороны), но
  баг S4 они поймать не могли: он жил в JS страницы, куда unit-тесты не достают.
- UI-smoke нашёл его на втором сценарии прогона — это главный аргумент за два
  уровня: разные классы багов живут в разных слоях.

## Что пришлось адаптировать

- Планировался прогон через Claude in Chrome (MCP), но расширение не подключено
  в этой сессии — уровень 2 переведён на Playwright (Python) с системным Chrome
  (`channel="chrome"`, без скачивания браузеров). Суть не изменилась: агент сам
  протыкивает UI, скриншотит каждый шаг и формирует отчёт; сценарии остаются
  текстом и не зависят от способа исполнения.
- Node.js на машине нет — потому Python-Playwright, а не JS.

## Цифры

| Что | Значение |
|---|---|
| Тестов уровня 1 | 33 (33/33 зелёные) |
| Smoke-сценариев | 5 |
| Прогонов smoke | 4 (v1: 3/4 → v1-rerun: 4/4 → v2: 5/5 → pr: 5/5) |
| Скриншотов | 69 |
| Реальных багов найдено smoke'ом | 1 (S4, починен профилем bug-fix) |
