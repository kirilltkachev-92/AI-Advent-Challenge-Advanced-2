# День 5. Пул задач execution loop (16 шт.)

Все задачи — про сервис `day 1`. Трекер — GitHub Issues (зеркало здесь).
Профиль выбирает харнесс по типу: bug → bug-fix, feature/refactor/tests →
day-builder, docs → day-docs, вопрос → research.
Критерий «сделано» у каждой задачи; верификация каждой — `./gradlew test`
зелёный (+ приёмка по критерию).

| # | Тип | Задача | Критерий «сделано» |
|---|---|---|---|
| 1 | bug | Баннер Main.kt при HISTORY_SIZE<=0 печатает «последние 0, в памяти» | Баннер пишет «история отключена»; при дефолте — как раньше; тесты зелёные |
| 2 | bug | Время в истории UI — сырой ISO (2026-07-23T10:45:44+03:00) | В `li.history-item` время формата ЧЧ:ММ:СС; тесты зелёные |
| 3 | bug | demo.sh не проверяет HTTP-коды: не-JSON уронит json.tool без объяснения | Каждый шаг проверяет ожидаемый код; при расхождении — понятная ошибка и exit 1 |
| 4 | refactor | Вернуть sealed MotivationResult (Done/Failed) в домен | Motivator возвращает sealed; HttpApi мапит Failed→502; API не изменился; тесты обновлены |
| 5 | refactor | /v1/history собирает JSON конкатенацией строк | Ответ через kotlinx.serialization, формат {count,entries} прежний; тесты зелёные |
| 6 | feature | GET /v1/limits | 200 {max_task_chars,max_body_bytes,history_size,model}; не-GET→405; тест |
| 7 | feature | /healthz: добавить history_count | Поле в ответе; тест обновлён |
| 8 | feature | Кнопка «Скопировать» у фразы (id=copyBtn) | Кнопка в PAGE, копирует фразу, тест страницы включает copyBtn |
| 9 | feature | DEEPSEEK_TIMEOUT_SEC (дефолт 60) для таймаута запроса клиента | Config+клиент используют; README env-таблица дополнена; тесты зелёные |
| 10 | feature | X-Request-Id: генерировать, отдавать заголовком, писать в access-лог | Заголовок во всех ответах; в логе id; интеграционный тест |
| 11 | tests | Вынести парсинг строки .env в чистую функцию + покрыть | parseDotEnvLine: комментарии/кавычки/пустые/без '='; ≥5 кейсов; Config использует её |
| 12 | tests | Дополнить HttpApiTest: 405 на /healthz (POST) и / (POST), 413 по Content-Length | Новые кейсы зелёные |
| 13 | tests | Sanitize фразы: снимать «ёлочки» и схлопывать пробелы + тесты | Motivator чистит «…», " ", \s+; тесты на кейсы; API прежний |
| 14 | docs | README day 1: секция «Тесты» | Как запускать, что покрыто, ссылка на smoke дня 3; команды проверены |
| 15 | docs | CHANGELOG.md для day 1 по git-истории | Хронология фич/фиксов по коммитам, без выдумок |
| 16 | research | Какие ветви HttpApi не покрыты интеграционными тестами? | Отчёт со списком файл:строка → day 5/research-gaps.md (пишет харнесс из ответа) |
