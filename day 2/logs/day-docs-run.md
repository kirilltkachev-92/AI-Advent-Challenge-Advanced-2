# Прогон профиля day-docs (v1) — первая попытка, один промпт

**Промпт** (дословно):

> day 1 — только что починен баг: HISTORY_SIZE=0 (и любое неположительное значение)
> теперь ОТКЛЮЧАЕТ историю (/v1/history отдаёт пустой список, /v1/motivate работает).
> Изменён HistoryStore.kt. Сверь документацию дня с кодом и приведи её в соответствие.
> REPORT.md дня 1 — это отчёт прошлой задачи про итерацию правил, его содержимое
> не переписывай, он не про сервис.

**Ответ агента** (полностью):

## Что обновил
`day 1/README.md` → 2 правки (код изменился — фикс HISTORY_SIZE<=0):
- пункт про `HistoryStore.kt` в «Как устроено»: было «кольцо последних 10 записей», стало «кольцо последних `HISTORY_SIZE` (по умолчанию 10) записей; `<= 0` отключает историю»;
- строка `HISTORY_SIZE` в таблице env: добавлено «`<= 0` — история отключена (`/v1/history` отдаёт пустой список)».

REPORT.md не тронут (отчёт про итерацию правил, история там только как пункт сценария прогона).

Поведение проверено вживую, не со слов: собрал (`compileKotlin` — ок), поднял сервис с `HISTORY_SIZE=0`, получил `/v1/history` → `{"count":0,"entries":[]}`, `/v1/motivate` → 200 с фразой, после чего история осталась пустой. `run.sh`/`demo.sh` соответствуют README (пути, порт 8080, шаги curl).

## Сверка env
Таблица сверена с `Config.kt` построчно — совпала полностью (7 переменных: `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL`, `PORT`=8080, `BIND_HOST`=127.0.0.1, `MAX_TASK_CHARS`=2000, `MAX_BODY_BYTES`=16384, `HISTORY_SIZE`=10; лишних строк нет, дефолты дословно). Рассинхрон был только семантический: таблица не отражала новое поведение `HISTORY_SIZE <= 0` — исправлено.

## Конфликты доков и кода
Противоречий нет: KDoc `HistoryStore` уже описывает «capacity <= 0 отключает историю», KDoc `Main`/`HttpApi` говорят про «последние 10» — это дефолт, коду не противоречит. Косметика (не конфликт, код не трогал): стартовый баннер `Main.kt` при `HISTORY_SIZE=0` печатает «последние 0, в памяти».

**Вердикт: рабочий результат с первого запуска.** 18 tool-вызовов, ~1,8 мин.
Находка агента («баннер печатает „последние 0“») — кандидат в bug-fix; в профиль v2
добавлена отдельная секция «Замечено в коде (не правил)».
